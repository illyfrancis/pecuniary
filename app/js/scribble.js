define([
    // 'jquery',
    'underscore',
    // '../lib/underscore-1.4.4'
    // 'bootstrap',
    'moment'
    // 'apps/Extension'
    // 'apps/EventBus',
    // 'apps/Repository',
    // 'apps/Mediator'
// ], function (/*$, _, Backbone, Bootstrap, moment, Extension, EventBus, Repository, Mediator*/) {
], function (u, m) {
    console.log('_ : ' + _.VERSION);
    console.log('u:', u);
    console.log('m:', m);
    // console.log('u : ' + u.VERSION);
});
